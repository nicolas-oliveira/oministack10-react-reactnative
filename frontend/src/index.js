import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';


ReactDOM.render(<App />, document.getElementById('root'));

// <App/> -> JSX (Javascript + HTML) torna a importação do react necessária
// DOM -> toda a arvore de elementos do html formada no client side (dev tools mostra ela)